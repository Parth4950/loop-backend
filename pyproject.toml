[tool.black]
line-length = 100
target-version = ["py313"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
# pyflakes (F) + pycodestyle (E) + isort (I): unused imports/vars, style, import order.
select = ["E", "F", "I"]
